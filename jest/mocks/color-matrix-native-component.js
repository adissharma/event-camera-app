// Mocks react-native-color-matrix-image-filters' native codegen spec for
// Jest. See jest.config.js for why this is needed — nothing under test
// renders the real native view, so a plain host component stands in for it.
module.exports = require('react-native').View;
